{% include 'default/_form.html.twig' with {
    page_title: 'title.create'|trans,
    form: form,
    submit_label: 'action.save'|trans,
    referer: referer|default('transaction_index'),
    back_to_list_path: 'transaction_index',
} %}