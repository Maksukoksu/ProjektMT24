{% include 'default/_form.html.twig' with {
    page_title: 'title.transaction_edit'|trans({'%id%': task.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    referer: referer|default('transaction_index'),
} %}