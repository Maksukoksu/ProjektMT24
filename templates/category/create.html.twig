{% include 'default/_form.html.twig' with {
    page_title: 'title.create'|trans,
    form: form,
    submit_label: 'action.save'|trans,
    referer: referer|default('category_index'),
    back_to_list_path: 'category_index',
} %}