{% include 'default/_form.html.twig' with {
    page_title: 'Edycja'|trans({'%id%': category.id|default('')}),
    form: form,
    submit_label: 'Zapisz'|trans,
    back_to_list_path: 'tag_index',
} %}