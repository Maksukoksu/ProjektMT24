{% include 'default/_form.html.twig' with {
    page_title: 'title.delete'|trans({'%id%': tag.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'tag_index',
} %}