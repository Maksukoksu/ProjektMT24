{% include 'default/_form.html.twig' with {
    page_title: 'Usuwanie'|trans({'%id%': wallet.id|default('')}),
    form: form,
    submit_label: 'Usuń'|trans,
    referer: referer|default('wallet_index'),
} %}